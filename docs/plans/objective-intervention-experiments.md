# Objective/intervention experiments

Compare how GRIT projection and prediction consistency use the same training-only
invariant pairs: ERM, V-REx, IRMv1, Fishr × vanilla, GRIT, consistency × CMNIST,
Waterbirds-CF. Preserve standalone methods, preparation, selectors and diagnostics.

## Review and decisions

- Initial worktree clean; only root AGENTS.md applies. README, config index and
  protocols reviewed. Before this extension, classes/validators restricted projection to GRIT.
- Shared trainer supports composition; Fishr already obtains statistic features
  through `prepare_features`. Keep legacy method IDs and add combination identities.
- MatchDG bias correction existed without an objective-version barrier protecting
  old tasks; added a targeted barrier for corrected searches.
- Saved head checkpoints relied on in-memory projection; projection state now persists.
- Paired summaries specialized to GRIT/ERM; generalized without changing old results.
- Existing discrepancy: root validation-only rule has explicit diagnostic test-oracle
  exceptions in protocols. Preserve that labeled track. CMNIST pair wording excluded
  its existing MatchDG penalty; corrected. README/config index also overgeneralized
  prefix budgets: only MNIST variants allow prefixes; Waterbirds requires all 240.
  These are documentation corrections, not new scientific changes.
- Protocols updated first: normalization, Fishr statistics and unchanged access.
- Nested composed algorithm config separates base objective and pair intervention;
  keep `rex` spelling for V-REx. No blocking scientific ambiguity identified.

## Steps and status

- [x] Inspect instructions/docs/trainer/lifecycle; update protocols.
- [x] Shared intervention and ERM complete synthetic lifecycles on both datasets.
- [x] V-REx, IRMv1, Fishr complete synthetic lifecycles on both datasets.
- [x] Planning, provenance, restoration, separate winners, status and W&B.
- [x] Paired reporting and incompatible MatchDG reuse protection.
- [x] Bounded matrix configs: 144 ERM / 576 each other row; 1,872 candidates per dataset.
  Commands and 3+2+10-seed task budgets in configs/README.md.
- [x] Focused math/access/lifecycle tests and required checks.
- [ ] Real-data validation pilots: blocked by missing configured artifacts; exact commands documented.

## Validation and completion

Require identity projection/zero strength equivalence, explicit logit penalty and
gradients, removed-direction invariance, Fishr statistics/warm-up/reset, training-only
pair access, selector separation, seed joins, complete predictor restoration and
standalone preservation. Run `uv run ruff check .`, `uv run basedpyright`,
`uv run pytest`, and synthetic complete lifecycles on both datasets.
Do not launch full production searches. Archive into docs/history only after agreed
validation including real-data pilots; otherwise keep outstanding evidence here.

## Pilot availability

No PROJECT_SCRATCH/GRIT_SCRATCH is set and no prepared artifacts exist under the
checkout's scratch root. Both ERM pilots stop before planning/training: missing
`scratch/artifacts/cmnist-none/dataset-manifest.json` and
`scratch/artifacts/waterbirds-none/construction/dataset-manifest.json`.
Real-data pilot evidence remains outstanding; use the exact loop in
[configs/README.md](../../configs/README.md#objectiveintervention-experiments) on the
prepared server, in ERM → V-REx → IRMv1 → Fishr order. Preserve full schedules so Fishr
crosses 1,500 updates. No production matrix has been launched.

## Completed local verification

- 61 trainer/baseline tests passed, including 15 focused intervention tests.
- All eight ordinary objective × dataset synthetic lifecycles pass, including Fishr
  after warm-up and both CMNIST selectors. Composed IRM diagnostic lifecycles also
  pass on both datasets and retain only explicitly diagnostic selection/test fields.
- Required Ruff and BasedPyright checks pass. Final full pytest run: 299 passed,
  one skip because CUDA is unavailable (694.84 seconds). No failures remain.
- W&B tests: 13 passed, including nested base/consistency coefficients. Its offline
  integration test requires local IPC and was run outside the socket-restricted sandbox.
- Candidate identity audit against pre-extension commit `6903666`: all 20 unrelated
  checked-in CMNIST/Waterbirds grids retain identical IDs; exact legacy
  reconstruction covers both older MatchDG
  grids for inspection, while current planning gives corrected MatchDG new identities.
- Review fixed historical checkpoint optional-field digests and old-summary resume
  compatibility. Actual completed ERM/GRIT trees generated by `6903666` on both datasets
  pass current status and resume with training hooks forced to raise if invoked.

The final scientific audit found no remaining implementation issues in pair access,
classifier parameterization, objective scaling, Fishr schedules/statistics, independent
selection, or restoration. Production grids remain provisional. Complete the real-data
pilots and review their validation behavior/runtime before freezing those grids; then
archive this plan under docs/history and update the README/docs index links.
